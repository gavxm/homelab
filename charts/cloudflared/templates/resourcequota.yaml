apiVersion: v1
kind: ResourceQuota
metadata:
  name: {{ include "cloudflared.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  hard:
    requests.cpu: 200m
    requests.memory: 128Mi
    limits.memory: 256Mi
    pods: "3"
