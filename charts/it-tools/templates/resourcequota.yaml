apiVersion: v1
kind: ResourceQuota
metadata:
  name: {{ include "it-tools.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  hard:
    requests.cpu: 100m
    requests.memory: 128Mi
    limits.memory: 256Mi
    pods: "3"
